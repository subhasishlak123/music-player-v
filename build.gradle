buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.2.2'
    }
}

// Keep the rest of the file empty except for the clean task
task clean(type: Delete) {
    delete rootProject.buildDir
}
